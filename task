User 

- error on missing phone number in profile not handled  
- improper phone number validation
- payment failed redirection is broken
- perform stock validation in checkout and on order creation
- remove coupon from cart when criteria breaks
- improve address input validation
- remove debug text shown in hero section in checkout (on hosting, or control this behavior with ENV)
- applying coupon of value higher than total shows the amount is deducted but it actually isn’t 
- ratings and reviews functionality is incomplete 
- wishlist doesn’t show price 
- wishlist doesn’t show the correct variant’s image 
- floor totals in cart
- design and show error pages properly 
- errors when applying referral code not communicated  (check logic)

Admin
- admin urls don’t redirect to admin login
- category input errors aren’t specific 
- improper category name validation causes the slug to break templates
- no proper path hierarchy in admin URLs
- highlight active item in sidebar
- when adding a new variant, either show the input fields on top or scroll the user to the form in bottom
- show submit buttons at the end of forms where the user will likely be after filling the form 
- for errors, display variant name, not variant code
- flat amount coupon value should be less than min purchase value 
- admin order cancellation (with confirmation)
- wrong tab/page titles 

General
- format emails
- use proper status codes 
- remove venv from remote
- don’t reset forms for validation errors
- merge to dev branch
- backend code has poor error handling: handle exceptions that are likely to happen, return proper user-readable text and use appropriate status code for the user agent.

Live coding performance not satisfactory. 
